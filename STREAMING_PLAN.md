# リアルタイムストリーミング翻訳 方針書

作成日: 2026-07-02

## 背景と目的

tamami-remote-client（モバイルを含む一般的な OS のマイク入力クライアント）から音声を
ストリーミングで受け取り、ASR + 翻訳の結果をクライアントへ送り返す構成を導入する。
クライアント側の出力は当面コンソール表示とし、将来は音声発話（TTS）や字幕表示など
複数のアウトプット方法へ拡張する。

SPEC.md 策定時の許容遅延「6〜12 秒」は旧仕様である。本方針書では
「可能な限りリアルタイム翻訳に近い体験」を目標として遅延目標を再策定する。

## 遅延バジェットの分解

エンドツーエンドの遅延は次の区間の合計となる。

| 区間 | 支配要因 | 現実的な値 |
|------|---------|-----------|
| 音声チャンク化 | チャンク長（Nemotron は 80ms〜1120ms 可変） | 0.1〜0.6 秒 |
| ネットワーク（往復） | LAN ならほぼゼロ、モバイル回線でも小 | 0.01〜0.15 秒 |
| ASR（ストリーミング） | Nemotron は発話終端 sub-100ms | 〜0.1 秒 |
| 翻訳単位の確定待ち | 文・節の区切りを待つ言語的な時間 | 1〜3 秒 |
| 翻訳推論（NLLB、GB10） | 1 文あたりの推論時間 | 0.1〜0.5 秒 |

支配項は計算でも通信でもなく「どこまで聞けば訳せるか」という言語的な待ち時間である。
ウクライナ語⇄日本語は語順が大きく異なる（SVO⇄SOV）ため、文の断片だけでの翻訳は
不安定であり、節や文の区切りまで待つことは品質上避けられない。人間の同時通訳者でも
原発話から訳出まで 2〜4 秒（ear-voice span）を要しており、これが物理的な下限に近い。

## 遅延目標（再策定）

単一の数値ではなく、段階的な出力ごとに目標を定める。

1. 認識テキスト（原語）の暫定表示：発話から 1 秒以内
2. 暫定訳の表示・更新：発話から 2 秒以内（後から書き換わることを許容する）
3. 確定訳：文・節の終端から 2〜4 秒以内
4. 音声出力（将来）：確定訳 + 1〜2 秒

体感上のリアルタイム性は確定訳の速さではなく、暫定表示が即座に現れて磨かれていく
ことで得る。文が伸びるたびに全体を訳し直して表示を差し替え、文末で確定させる
再翻訳（retranslation）方式を採用する（Google 翻訳のライブ翻訳と同方式）。

## アーキテクチャ

```
[クライアント] --(WebSocket 上り: raw PCM 16kHz/mono/s16le, 160〜320ms フレーム)--> [tamami]
[クライアント] <--(WebSocket 下り: JSON 認識・翻訳結果, 暫定/確定セマンティクス)---- [tamami]
```

- tamami 側：WebSocket サーバー → Nemotron 3.5 ASR（ストリーミング）→ NLLB（再翻訳方式）
- クライアント側：マイク入力 → 送信 ／ 受信 → `TranslationOutput` 抽象
  （まず `ConsoleOutput`、将来 `TTSOutput`・`SubtitleOutput` を追加）

## ワイヤープロトコル（骨子）

詳細は `PROTOCOL.md`（今後作成）に定義する。

### 上り（音声）

- WebSocket バイナリフレーム
- raw PCM s16le / 16000Hz / mono を 160〜320ms 単位で逐次送信する
  （バッファを溜めて送らない）
- フレームに送信時刻（クライアント時計）を付与し、遅延計測に用いる
- Opus 化は第 2 段階とする（raw PCM は 256kbps 程度であり LAN では問題にならない）

### 下り（結果）

JSON テキストフレーム。

```json
{
  "type": "asr",
  "segment_id": 12,
  "text": "...",
  "lang": "uk",
  "is_final": false,
  "ts_audio_start": 34.2
}
```

- `type` は `"asr"`（認識テキスト）または `"translation"`（訳文）
- 同じ `segment_id` のメッセージは前の内容を置き換える
- `is_final: true` で当該セグメントを確定する
- コンソール出力は行の書き換え + 確定時の改行で対応する。字幕表示・TTS も
  同じセマンティクスの上に実装する（TTS は確定訳のみ読み上げる、など）

## 設計判断

| 論点 | 判断 | 理由 |
|------|------|------|
| トランスポート | WebSocket | ネットワークは遅延バジェットの 1〜2% に過ぎない。WebRTC が必要になるのはモバイル回線越しで 1 秒未満を狙う段階であり、当面は不要。差し替えに備えトランスポート層のみ抽象化する |
| 音声コーデック | raw PCM（当面） | 両端のコーデック実装を省略できる。帯域は LAN では問題にならない。Opus 化する際はパケット単位のエンコード（opuslib / PyAV）が必要（既存の ffmpeg ファイル書き出しは流用不可） |
| ASR | Nemotron 3.5 ASR に直行 | Whisper の疑似ストリーミング（チャンク分割 + VAD）はそれだけで 2〜5 秒を要し、新目標では成立しない。Whisper 経由の暫定接続は行わない |
| 翻訳方式 | 再翻訳方式 | ASR 更新のたびに文全体を訳し直して表示を差し替え、文末で確定する。NLLB の速度（1 文 0.1〜0.5 秒）と相性が良く、語順差の大きい言語対でも品質を保てる |

## 着手順

1. `PROTOCOL.md`：暫定/確定セマンティクス・segment_id・タイムスタンプを含む
   ワイヤープロトコルの定義
2. tamami 側エコーサーバー + クライアント送受信部 + 往復遅延の計測表示
   （ASR 接続前に通信路を開通させる）
3. クライアント側 `TranslationOutput` 抽象 + 書き換え対応の `ConsoleOutput`
4. tamami 側：Nemotron ストリーミング ASR 接続（暫定テキストをそのまま下りへ流す）
5. tamami 側：NLLB を再翻訳方式で接続（文末で確定訳を送出）
6. チャンク長・再翻訳頻度・区切り判定のチューニング（実測と目標値の差分を詰める）

常にエンドツーエンドで動く状態を保ちながら段階的に置き換えていく。

## 計測方針

目標値を掲げる以上、計測なしでは改善が回らない。疎通の段階から以下を維持する。

- クライアントは音声フレームに送信時刻を付与する
- サーバーは結果 JSON に各段（受信・ASR・翻訳・送信）の処理時刻を載せる
- クライアント側で目標値との差分を常時観測できるようにする

## 将来の拡張

- Opus 化（帯域が制約になる回線での運用時）
- モバイルブラウザクライアント（getUserMedia は 48kHz が標準のためリサンプリングが必要。
  localhost 以外では HTTPS が必須）
- `TTSOutput`（確定訳の読み上げ）・`SubtitleOutput`（字幕風表示）
- モバイル回線越しで 1 秒未満を狙う場合の WebRTC / WebTransport への差し替え
